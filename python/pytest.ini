[pytest]
testpaths = tests
markers =
    clinical: SaMD reference cases — failures block rule_pack release
    unit: pure-logic unit tests (rule DSL, agent, i18n)
    integration: full HTTP round-trip against FastAPI TestClient
filterwarnings =
    ignore::DeprecationWarning
