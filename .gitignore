# Сборочные артефакты (генерируются из содержимого репозитория, в git не хранятся)
*.zip
/dist/

# Временная сборка кабинета из кусков (не хранится в git)
/.cabinet_build/

# Прочее
.DS_Store
METACOD-knowledge-base.zip
*.zip
